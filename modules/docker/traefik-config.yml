api:
  dashboard: true
  insecure: true
  debug: true
entryPoints:
  web:
    address: ":80"
    #http:
    #  redirections:
    #    entryPoint:
    #      to: websecure
    #      scheme: https
  websecure:
    address: ":443"

providers:
  docker:
    exposedByDefault: false

certificatesResolvers:
  letencrypt:
    acme:
      email: youremail@email.com
      storage: acme.json
      #dnsChallenge:
      #  provider: cloudflare
      #  delayBeforeCheck: 10
      httpChallenge:
        entryPoint: web
