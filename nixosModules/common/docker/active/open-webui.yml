services:
  openwebui:
    image: ghcr.io/open-webui/open-webui:ollama
    ports:
      - 127.0.0.1:3030:8080
      - "${TAILSCALE_IP}:3030:8080"
    volumes:
      - $STATE_DIRECTORY/open-webui:/app/backend/data
      - $STATE_DIRECTORY/open-webui/.ollama:/root/.ollama
    restart: unless-stopped
